> 图谱：[[_MOC-面试知识图谱]] · 对照复盘：[[面试复盘]] · [[概念-RAG混合检索]] · [[概念-LangGraph状态机]] · [[概念-LoRA微调]] · [[概念-Golden评测]]
Google Ai studio：

如果你是一个资深的ai agent应用工程师，而我准备面试初级或中级岗位，你会问我哪方面的问题。结合我的简历。询问

你好！作为一名资深的 AI Agent 应用工程师，看到你的简历，我发现你的项目背景非常扎实：既有**工程落地（LangGraph + RAG）**，又有**算法微调（Qwen2.5 LoRA）**，且业务场景（医疗导诊）对逻辑严密性和准确性要求极高。

针对初级/中级岗位的面试，我不会只考你会不会用框架，而会重点考察你对**“为什么这么做”的思考**、**复杂问题的解决思路**以及**系统落地的细节**。

以下是我会问你的几个核心维度的问题：

------



### 一、 关于 Agent 架构与 LangGraph (考察逻辑控制)

你的简历提到使用了 LangGraph 构建状态机，这是目前 Agent 开发的主流趋势。

1. **状态管理：** 在医疗导诊这种多轮对话中，你是如何设计 State 结构的？如果用户中途突然切换话题（例如：问着感冒突然问医院停车费），你的 LangGraph 节点是如何跳转或“回流”的？

   增加挂起的任务，改变appstate，标记问诊为false，指南为true，走intent路由，分配es查询，得到结果后，改变appstate，返回diagnose，并展示结果后，继续槽位问询。增加任务列表，触发最新任务

2. **槽位填充逻辑：** 你提到了“根据未完成槽位自动触发追问”。请问这个逻辑是硬编码在 Graph 的节点中，还是由 LLM 动态决定下一步？如果是 LLM 决定，如何防止它反复询问同一个已答复的问题（Dead Loop）？

   通过槽位来控制，槽位匹配到，则会下一个问题，避免deadloop

   系统使用 slot 状态驱动问诊流程。LLM 只负责 slot 抽取和自然语言生成，Graph 节点通过检测未完成 slot 决定是否追问，从而避免模型反复询问已回答的问题

3. **Human-in-the-loop：** 简历里写了支持人工介入。具体在什么场景下会触发人工？是基于模型置信度阈值，还是特定的流程节点？

   ​	关键词匹配，会直接报急诊或人工电话介入，没有基础模型判断，是特定流程节点。同时，slot节点llm+lora会增加症状组合触发急诊的样本

### 二、 关于 RAG 优化 (考察召回与精准度)

医疗领域的 RAG 容错率极低，我会深挖你的双通道检索设计。

1. **混合检索（ES + Milvus）：** 为什么需要 ES？在医疗场景下，哪些信息是 Milvus（向量）容易搜错而 ES（关键词）能搜对的？你的 Rerank 环节是如何融合这两者得分的（Reciprocal Rank Fusion 还是模型打分）？

   milvus解决的是像不像的问题（肚子疼--腹部疼痛），而es解决的是 是不是的问题（流程问题，实体信息，医学术语），rrf，（1/60+ + 1/60+）

2. **Query Rewrite（查询改写）：** 医疗术语存在大量“俗语”与“专业术语”的差异（如：拉肚子 vs 腹泻）。你的改写逻辑是基于 Prompt 还是有专门的 Term Mapping？当改写后依然召回不到内容，你的系统会如何反馈给用户？

   ​	系统先通过 slot extraction 获取症状，再通过 Term Mapping 做医学术语归一化（也接近俗语），建立映射表。如果归一化后仍未召回内容，会触发 query rewrite 模型重写查询并最多重试三次，仍然无法命中时返回无法找到相关资料的提示。

3. **多路召回的评价：** 你提到召回率提升了 15%-20%，这个“测试集”是怎么构建的？如何量化评价 RAG 环节的“幻觉”问题？

    平均分布类，每一类50个问题，问题对应chuckID，大约500个左右，定义recall@5为命中正确文档/总query数。前5有一个命中即可。如果回答中的关键结论 **无法在文档中找到证据**，判定为 hallucinatio。用大模型判断promt，答案是否完全有doc支持
    
    4，常见问题映射处理，检索和生成答案部分

### 三、 关于模型微调 (考察算法深度)

你针对 Qwen2.5 进行了 LoRA 微调，这是很务实的选择。

1. **微调 vs Prompt Engineering：** Qwen2.5 本身指令遵循能力已经很强了，为什么还要大费周章做 5000 条数据的微调？是因为 JSON 格式不稳定，还是领域词汇理解不到位？

   我的理解就是promt工程有可能出错，输出不稳，加强专业领域知识学习达到大模型的效果。为啥promt会出错，lora就不会。口语到术语 结构化负样本：划定行为边界 严格的 Schema 拟合训练

   数据类别	作用	示例
   1. 基础症状描述	学习提取主要症状	"我头痛" → symptom: 头痛
   2. 复杂问句	学习否定、条件、多意图	"我不舒服但没发烧" "如果咳嗽怎么办"
   3. 俗语→术语	学习领域词汇映射	"肚子疼" → "腹痛"
   4. 紧急词识别	学习危险信号	"胸口疼得厉害" "喘不上气"
   5. 完整槽位	学习多字段结构化输出	"发烧三天，咳嗽，有糖尿病" → 多槽位
   6. 追问回复	学习追问后的回答	问"持续多久"答"两天了"

2. **困难样本处理：** 简历提到“否定表达”和“隐喻表达”。请举个例子：模型在微调前面对什么样的否定表达会出错？你微调后是如何解决这个问题的？

   嗓子像着火一样疼" 我咳嗽两天了，没有发烧"，编写正确和错误样本数据，加强训练来解决该问题

3. **评估指标：** Frame Accuracy（整条结构化准确率）达到 0.81，那剩下的 19% 错误通常集中在什么地方？是字段缺失还是内容填错？

   hardcase出错，更关心chief症状是否正确获得

   我没有糖尿病，也不是高血压，但是最近体检说血糖偏高一点，如果不控制会不会变成糖尿病？

   "感觉胸口像压了一块大石头，喘气都费劲，已经持续一周了，之前稍微好一点，现在又严重了"

   "昨天吃了烧烤，今天早上起来就拉肚子，一共拉了五六次，都是水样的，没力气，感觉要虚脱了"

   "隔壁床说这个药效果不错，我也想试试，但我吃了好像又不太舒服，你们医院有这个药吗？"

   "头疼死了，疼起来跟针扎一样，晚上也睡不着，一直在想这个病会不会好，都快抑郁了"

   > 1. 字段缺失（约 50%）：复杂句式导致部分槽位未识别
   > 2. 内容偏差（约 30%）：否定表达、近义词理解有误

### 四、 关于工程化与性能 (考察落地能力)

1. **并发与异步：** FastAPI 结合 LangGraph，你是如何处理长耗时请求的？如何保证 Redis 存储的会话状态在并发访问时的一致性？sesionmanger管理，redisver保持会话状态，通过槽位，timeout限制超时
2. **MCP（Model Context Protocol）：** 你提到了 MCP 调用外部工具。请问你接入了哪些医疗外部工具？如何处理工具返回的超长数据，避免超过上下文窗口？得到的信息是严格要求json格式，筛选出所要的关键字，太长的值进行摘要压缩。es哪里导入建库的时候，已经做了数据清洗。
3. **冷启动与延迟：** 首屏渲染（TTFT）大概是多少秒？对于长链路的 Agent（意图识别->槽位抽取->RAG->生成），你是如何优化用户等待体验的？3s，增加stream输出。可以通过加机器，减少检索文档，用小规模的模型来提高响应速度，取决于c端还是b端，该项目是b端，以质量为先。3到5s即可，最后一定是高质量模型兜底答案

### 五、 场景模拟题 (考察潜力和解决问题能力)

**场景：** 用户输入非常模糊，比如“我感觉全身不舒服”。

- 你的 Agent 流程会先走 RAG 检索，还是先走槽位抽取的追问？

  槽位提取，再走rag搜索

- 如果 RAG 检索回来的医疗建议和 LLM 微调学到的知识冲突了，你的系统以哪个为准？为什么？

   LLM 微调只用于slot提取，不用于知识学习，llm-anser严格按照rag和es的文档为准，没有得到文档，回答没搜到，不会强制回答问题

------



### 给你的面试建议：

1. **准备好具体的 Case：** 准备 1-2 个“起初效果很差，我通过调整了 XX 策略（比如改了 Prompt 结构或加了 Rerank 权重）后变好了”的故事。
2. **量化指标背后的逻辑：** 不要只记 85% 召回率，要准备好回答这个 85% 是怎么算出来的（评测集怎么打标的）。
3. **理解医疗安全性：** 这是一个加分项。主动谈谈你如何防止 Agent 给用户开具错误的处方建议（比如加入“仅供参考，请咨询医生”的强制约束节点）。

你的简历非常符合当前企业对 **“能写代码、懂模型原理、能用工具实现闭环”** 的中级 AI 工程师的需求，祝你面试顺利！

如果岗位是 **AI Agent 应用工程师（初级或中级）**，面试通常围绕五个能力域。重点在系统设计能力、LLM机制理解、工程实现能力、稳定性治理。下面是企业常见的提问结构。

------

下面按 **能力域**对全部问题进行归类整理，仅保留问题本身。

------

一、LLM基础与生成机制

1. Token 是什么，为什么会影响推理成本---单个词通过embeding变成token，是llm上下文的最小单位，调用api需要通过token来计费，实际上就调用接口的输入和输出

2. Embedding 的作用是什么 --- 变成大模型能理解的机器语言，词向量，项目中的用的是text-embeding -v2 , qwen系列

3. 为什么向量检索通常使用 cosine similarity  --- 

   ```
   Similarity=cos(θ)=∥A∥∥B∥A⋅B
   ```

   

    ，归一化的话，分母等于1，直接算点集，意义就是像不像的问题

4. Transformer 中 Self-Attention 的计算流程是什么  --- 单头Attention(*Q*,*K*,*V*)=softmax(*d**k**Q**K**T*)*V* --- 并接 * O*融合后就是多头

5. 为什么当前大模型多数采用 Decoder-only 架构 自己已经积累了大量上下文关系，不需要encoder再kv，使用自己的kv就够了

6. Temperature 和 Top-p 对生成结果有什么影响 --- ffn成softmax（p/t）, top-p是阈值，大于它才会采样

7. LLM 为什么会产生幻觉  --- 他是一个概率推演和语言连贯，预测采样输出，不是一个严格的逻辑选择，可验证，事实正确，上下文过长，只关注最初的和最新的，中间的信息会出现注意力稀释，出现忘记的又胡说的情况，学习的数据，本身就有错误的信息。T为1，本身就允许不同的答案出现

8. 如何降低模型幻觉  rag可以将从互联网学习的参数模型中，限定在外部检索系统，rerank排序，上下文截断，promt强调只从rag给的文档回答，没有就说不知道，降低温度，topp，topk，模型只负责生成合理句子和提取关键信息，验证环节使用工具来获得信息验证，比如病历，排班表，知识图谱。。llm校验是否符合事实，不符合，触发重写机制。特殊情况，比如说多个与或非结合，暗喻，多个症状组合触发急诊等垂直知识，需要lora强化模型输出结果。强调输出为json schema，不许输出其他内容

9.  为什么模型会出现 instruction drift指令漂移  自回归生成导致早期指南减弱，远距离token权重下降，多轮对话，上下文互为矛盾干扰，噪声太多，传的数据就是错误的。--使用系统提示固定规则，多个上下文，自我校验，不匹配结果重写生成

10. 为什么长对话容易出现上下文遗忘，注意力稀释导致遗忘，较近原则，太长会压缩，链式传导信息，会导致误差放大---摘要，分段，重复关键指令，限制上下文，保留关键事实

11. 为什么需要 embedding 模型  -- embedding 的核心用途是 **语义匹配**。文本向量化后，可以用向量距离判断相似度，方便后续的于一搜索，rag的最小单元，构建语义空间，文本之间的关系可以通过向量距离体现出来

12. embedding 模型如何选择 ---中英语，专业领域，编码和查询维度一致，embt排行榜，可以训练的模型

13. 为什么需要 Prompt 工程，

    1 定义任务类型
     2 控制模型行为
     3 减少幻觉
     4 引导推理过程
     5 消除指令歧义
     6 约束输出结构
     7 支持 Agent 工作流

14. Prompt 工程和模型微调的边界在哪里 任务逻辑简单 模型已有能力 只需要改变输出格式或行为，基础模型能力不足，原有通用知识不覆盖，需要学习垂直知识，稳定执行复杂任务。

涉及核心架构
Transformer

------

二、RAG系统设计

1. 什么是 RAG 系统  **检索外部知识并结合语言模型生成能力**的系统架构。它使语言模型能够利用最新或私有数据，并显著降低幻觉问题。

2. RAG 系统的完整流程是什么--- 文档库建立向量库（类型，），更新索引，输入query-embeding，查询相似度最高的前10个并返回

3. RAG 中 chunk size 应该如何确定 --- 语义完整避免主题混杂 控制上下文长度 200 到 400 tokens 是稳定区。或100 200 300作测试recall@5

4. RAG 检索中的 TopK 如何确定 参数 topk * chucksize 500 小于上下文RAG context ≤ 2000 tokens，主要rerank小于512 tokens的模型上下文长度，--- llm的长度

5. RAG 中 chunk overlap 为什么重要 embedding 模型通常在 **完整语义单元**上效果最好，chuck语义不完整的话， 一半在这里，一半在下一个，检索的时候，都无法被收到。50个字

6. 如何提升 RAG 的 recall。数据部分错误样本修正，扩大训练样本  向量检索，查询改写，检索架构 

7. 为什么需要 rerank

   像不像，是不是的问题要综合解决，提高召回率

8. 什么是 Hybrid Search

   粗排--融合 -- 精排 bge qwen3 

9. RAG 检索结果如何构造最终 prompt，

   context，问题入口，文档/资料/对话历史，从给的资料中回答，并标记来源，无法回答则回复不知道，

10. RAG 系统最常见的性能瓶颈在哪里---- 生成质量 ：切片，语义空间（不是专业的embeding），上下文窗口太长导致中间失落，数据没法及时同步更新es和向量库，响应延迟：llm调用就很耗时，重拍，大规模向量搜索，参数没及时更新，导致搜索极为缓慢

11. RAG 召回正确但回答仍然错误时原因可能是什么 --- 数据本身查看下是不是错的

12. 如何判断问题出在 RAG 召回阶段还是生成阶段 -- rag给的文档能否解决问题

13. 如何评估 RAG 系统效果 -- 检索部分的recal topk中相关文档比例 mRr，端到端的准确率（另外一个llm判断打分），首字响应时间2s以内

14. 如何构建 RAG 的评估数据集 --- 粗排后，人工介入标注哪个答案正确，

15. 如何构建困难样本测试集---**20% 的简单题（基准）**、**60% 的中等/困难题（拉开差距）** 和 **20% 的陷阱/拒答题（安全性）**  6成来自于语义相似但不同科室/场景 

部分匹配（只有部分信息相关）同类检查容易混淆 表达模糊 多跳推理

涉及方法
Retrieval-Augmented Generation

------

三、向量数据库与检索技术

1. 向量数据库和传统数据库有什么区别 像不像，关系和非关系，解决不了像不像的问题
2. HNSW 索引的原理是什么  从顶端到中 到底层
3. Milvus 与 FAISS 在生产环境中的差异是什么  数据几乎不更新（如离线特征库，需要更新，就milvs
4. Elasticsearch 与向量检索如何融合 加权和rrf融合
5. 向量检索中的 TopK 如何确定  **计算公式**：TopK = (LLM 窗口大小 - Prompt 模板长度 - 预留生成长度) / 平均分片（Chunk）长度 固定 **TopK = 5**，简单直接。**动态阈值**（如 Score > 0.65） + **TopK_max=8**。

涉及系统

- Milvus
- FAISS
- Elasticsearch

涉及算法
Hierarchical Navigable Small World

------

四、Agent系统架构

1. 什么是 AI Agent --- **拥有目标、会使工具、能自我迭代的自动化数字助手**
2. AI Agent 与普通 LLM 应用有什么区别 任务涉及多个外部系统、需要多步推理、环境是变化的、目标是开放性的。
3. Agent 系统通常包含哪些核心模块 统通常遵循 **“大脑 + 规划 + 记忆 + 工具”** 的经典架构
4. Tool calling 是如何实现的  **Tool Calling = 语义理解（LLM） + 结构化输出（JSON） + 外部执行（你的代码） + 结果汇总（LLM）。**
5. 为什么需要 function calling 操作单元
6. Agent 如何避免无限循环调用工具 限定次数，时间，token，promt：每次重试都尝试不同的方法，让工具学会拒绝模型无意义的重试。
7. 为什么 Agent 系统需要状态机 逻辑必须是**确定性**的
8. Agent 多轮对话如何管理 sesion*redis处理，独立空间和appstate
9. Agent 系统为什么需要 memory  上下文，处理问题的依据
10. 如何设计 Agent 状态机 invoke
11. 如何避免 Agent 无限循环调用工具 

涉及框架
LangGraph

------

五、工程实现与系统架构

1. 如何管理多轮对话 session 先登录id，ridis处理只在这个id的空间里运行程序，不与其他的id关联
2. 为什么使用 Redis 存储会话状态 不同的空间
3. 并发情况下如何保证 session 一致性
4. 如何处理长耗时工具调用 限定时间，次数，token强制结束，log预警，
5. 如何控制上下文长度 压缩，截断，摘要
6. 如何降低 LLM 调用成本 小功率用小模型，没必要每个都用高级别模型，缓存存常见问题，减少调用大模型，减少对话历史和检索给的值，精简promt
7. 如何进行 prompt 版本管理
8. 如何进行日志记录与系统 trace 挂langfuse
9. 如何设计一个高并发 AI Agent 系统
10. AI Agent 系统如何做成本控制
11. AI Agent 系统如何进行日志与监控
12. AI Agent 系统如何进行模型部署
13. AI Agent 系统如何进行工具扩展

涉及技术

- Redis
- FastAPI
- Docker

------

六、模型微调与LoRA

1. 什么情况下需要微调模型 垂直知识和特殊任务
2. LoRA 的原理是什么 小矩阵挂大模型，训练
3. LoRA 为什么只训练少量参数 
4. LoRA 通常覆盖哪些网络层
5. 为什么槽位抽取适合使用 LoRA 微调

涉及方法
Low-Rank Adaptation

------

七、项目设计与业务逻辑

1. 为什么系统需要使用 RAG
2. 向量检索已经返回结果时为什么还需要 rerank
3. 为什么企业通常采用 Hybrid Search
4. 为什么需要设计槽位抽取
5. 为什么不使用规则进行槽位抽取

------

八、医疗 AI Agent 场景

1. 如何设计医疗导诊 AI Agent
2. 医疗导诊系统如何进行分诊科室判断
3. 医疗导诊系统如何识别风险信号
4. 医疗导诊系统如何评估导诊准确率
5. 医疗对话数据如何构建训练集
6. 如何处理否定表达的医疗对话
7. 如何处理隐喻表达的医疗对话

------

九、系统排错与调试

1. 如何排查 AI Agent 系统回答错误的问题
2. 如何排查 RAG 召回为空的问题
3. 如何排查上下文超长问题
4. RAG 召回正确但答案错误时如何排查
5. Agent 工具循环调用时如何排查

十、Transformer基础

1. 什么是 **Transformer**
2. Transformer 为什么能够替代 RNN
3. Transformer 的整体结构是什么
4. Encoder 和 Decoder 的区别是什么
5. 为什么当前大模型大多使用 Decoder-only 架构

------

二、Attention机制

1. Self-Attention 的计算流程是什么
2. 为什么 Attention 需要 Q、K、V
3. Attention 的计算公式是什么
4. 为什么要除以 √d
5. Attention 的时间复杂度是多少

------

三、多头注意力

1. 什么是 Multi-Head Attention
2. 为什么需要多头注意力
3. 多头注意力的计算流程是什么
4. 多头注意力为什么最后需要一个线性投影层

------

四、Transformer内部结构

1. Transformer 中 FFN 层的作用是什么
2. 为什么每一层都有 Residual Connection
3. LayerNorm 的作用是什么
4. 为什么 Transformer 需要 Position Encoding

------

五、推理过程

1. Decoder-only 模型生成文本的流程是什么
2. KV Cache 是什么
3. KV Cache 为什么能加速推理

------

六、工程相关问题

1. 为什么长上下文会导致性能下降
2. Transformer 为什么容易出现上下文遗忘
3. Transformer 为什么容易产生幻觉
4. Attention 计算为什么会导致显存占用高

实战面试：

ai团队 ---  ui，后端架构，维护，ai算法，

embeding是怎么出来的  token -- attention  -- last/cls/pool -- 代表所有语义，压缩到多维度空间里

如何推荐一个ai项目

qps三千，架构怎么处理这事

langgraph的state如何定义，mcp工具，fastapi鉴权如何做，