---
title: 0806-AI开发工程师面试复盘
date: 2026-08-06
tags:
  - 面试复盘
  - AI开发工程师
  - Agent
  - Claude-Code
  - Codex
source: BOSS直聘JD、机试及技术面录音转写
---

# 0806-AI开发工程师面试复盘

> [!warning] 说明
> 本文根据自动转写整理，原文有较多同音字和术语识别错误。面试已通过机试，机试后的技术交流约 20 分钟。

## 一、JD 要的人

这个岗位要的不是纯算法研究员，也不是只会调用大模型 API 的开发，而是：

> **能把模糊需求独立做成可用产品的 AI-Native 全栈/产品型工程师。**

核心画像：

1. Python 或 TypeScript 工程基础扎实，能自己写代码和排查问题。
2. 有 Claude Code 或 Codex 的真实项目经验，不只是聊天和复制代码。
3. 懂 ReAct、Plan-and-Solve、Tool Calling、Memory 和 Agent Loop。
4. 了解 LangGraph、Dify、AutoGPT 等框架，有二次开发经验更好。
5. 可以覆盖“需求分析 → 产品设计 → 技术方案 → AI 协同编码 → 测试 → 交付”的完整闭环。
6. 在小团队中高自主工作，能直接和业务方、产品人员对需求。
7. 拒绝“纯 PPT 架构师”，最终看实际代码和独立交付。

预估权重：

| 能力 | 权重 |
|---|---:|
| 工程编码与独立交付 | 35% |
| AI 编码工具实战 | 25% |
| Agent 原理与框架 | 20% |
| 需求分析和产品意识 | 15% |
| Git、英语文档和协作 | 5% |

## 二、本轮结论

- 机试：**已通过**，说明编码和基本交付能力已获得初步认可。
- 技术面：约 20 分钟，主要是验证项目真实性、工程判断和 Agent 理解。
- 本轮通过概率：**约 50%**，合理区间 **40%–60%**。
- 如果原定就是 20–30 分钟的复核面，时长不是负面信号。
- 如果原定 45–60 分钟却提前结束，则偏负面。
- 面试官最后表示还要横向比较候选人，目前更像“保留候选”，不是明显强通过或当场淘汰。

## 三、面试问题清单

### 1. 之前公司做的是什么项目？

**回答要点**

- 医疗项目，涉及病例筛查、医疗知识和 RAG。
- 使用 Python 开发，过去也有测试开发经验。

**复盘**

项目方向与岗位相关，但转写中项目的用户、业务价值、本人职责和量化结果没有形成简洁闭环。

### 2. 你写过 Java 吗？Python 和 Java 的并发模型有什么区别？

**现场表现**

回答了 Java 多线程可以利用 CPU，Python 偏事件驱动，但没有准确说明 GIL、CPU/IO 密集任务、`asyncio` 和多进程的边界。

**标准回答**

> CPython 的 GIL 会限制多个线程并行执行 CPU 密集型 Python 字节码，但不等于 Python 无法高并发。IO 密集任务适合 `asyncio` 或线程池，CPU 密集任务适合多进程、原生扩展或独立计算服务。Java 线程通常可以直接并行利用多核，但也有线程调度和上下文切换成本。应根据负载模型选择，不是简单判断谁更好。

### 3. 机试项目中，删除文件时先删文件还是先删数据库？

**面试官连续追问**

- 文件删除成功，数据库挂了怎么办？
- 数据库修改成功，文件删除失败怎么办？
- 数据库事务能不能回滚文件系统操作？
- 服务中途宕机怎么恢复？
- 长时间停留在“删除中”的记录怎么处理？

**现场表现**

开始回答“先删文件，再删数据库”，被追问后承认没有完整考虑。状态机、重试和补偿方案主要是面试官引导出来的。这是本场最明显的工程失分点。

**标准回答**

> 删除请求先在数据库事务中把状态从 `ACTIVE` 改为 `DELETING`。事务提交后，由异步任务删除文件。文件不存在也视为成功，保证幂等。成功后把状态改为 `DELETED`，失败则记录次数并重试。定时任务扫描长时间停留在 `DELETING` 的记录，进行补偿、对账或告警。数据库事务无法直接覆盖文件系统，因此采用状态机和最终一致性，必要时结合 Outbox、消息队列、软删除或对象存储版本。

### 4. 你用 AI 写代码多久？有哪些经验和坑？

**回答要点**

- 从去年 12 月左右开始深度使用 Claude Code 等工具。
- 不能一次性把大需求全部交给 AI，否则容易得到“看起来很美好”但不可靠的结果。
- 使用 SDD、Plan 和 Skill 先完成规划，再分步执行。
- 需求、实现和验证不能全部托管给 AI。
- AI 可能自己伪造数据，让测试“假通过”，必须人工检查真实链路。
- 顶级模型负责计划和审查，其他模型可以负责执行。

**复盘**

这是本场最强的回答，与 JD 中 Claude Code/Codex、Skill 和 AI-Native 开发经验高度匹配。

### 5. 除了医疗，以前还做过什么类型的项目？

**现场表现**

这里因现场打断，后续话题转移，没有听到完整回答。

**后续准备**

选择 2–3 个与岗位相关的项目，每个只讲：用户和问题、本人职责、技术选型、交付结果。

### 6. 你怎么看 RAG 和现在 Agent 渐进式搜索的区别？

**面试官的追问**

- Claude Code/Codex 没有把代码库全部向量化，为什么仍然可以理解代码？
- 长上下文或 Agent 主动读文件能不能替代 RAG？
- 两种方案在准确率、成本、速度和可评测性上有什么区别？
- 为什么传统 RAG 的召回率很难达到 100%？

**现场表现**

表达了“大模型上下文变大后，很多场景不一定需要 RAG”，但没有完整拆出 RAG、Tool Calling、Agent Loop 和上下文管理的边界，后续主要由面试官讲解。

**标准回答**

> RAG 是先从大规模语料中检索候选证据，再将证据放入上下文生成结果，适合大规模、低延迟和低成本检索。Agent 渐进式搜索由模型根据中间结果反复选择目录、文件和工具，适合代码库、结构化目录和需要多步判断的任务。长上下文和 Agent 搜索可以替代一部分简单 RAG，但不能普遍替代。Agent 搜索通常更灵活，但更耗 Token、延迟更高，路径也更难固定评测；二者可以组合使用。

### 7. Agent Tool Use 的本质是什么？

**现场表现**

这一题没有形成完整回答。面试官表达了“看来你对 AI Agent Tool Use 这套东西不是很了解”，随后解释了渐进式搜索。

**标准回答**

> Tool Use 的本质是模型根据当前目标和状态，输出结构化的工具名和参数；外部程序真正执行工具，再将 Observation 返回给模型。模型根据结果继续决策，形成 ReAct/Function Calling Loop。工程上还要约束工具权限、参数校验、超时、重试、最大步数、幂等和人工确认。

### 8. 你对我们还有什么想了解的？

**现场回答**

询问了工作节奏和岗位职责。面试官说明：团队的产品经理角色被弱化，工程师需要自己分析需求、设计、实现和测试，独立负责某个完整功能。

**下次可以追问**

- 团队现在主要是 Workflow 型 Agent，还是自主规划型 Agent？
- 这个岗位前三个月最重要的交付目标是什么？
- 团队如何评测 AI 功能的正确性和稳定性？
- 机试项目如果进入生产，团队最希望优先完善哪一部分？

## 四、优势和风险

### 明显优势

- 机试已通过，有可运行的实际交付作为证据。
- 有真实 Claude Code/Codex 工作流，不是只会写 Prompt。
- 能说出 SDD、Plan、Skill、分步执行和人工验证的实际价值。
- 注意到 AI 会伪造数据、生成“假通过”测试，这是真实的工程经验。
- 医疗/RAG 项目与 AI 岗位有一定相关性。

### 主要风险

- 跨资源一致性设计缺少主动考虑，影响“扎实工程能力”的评价。
- Python/Java 并发的概念和边界表达不准确。
- RAG、长上下文、Agent 搜索和 Tool Calling 的概念混在一起。
- 面试官的纠正和讲解多于技术认可，没有建立强技术主导权。
- 没有展示出明确的 LangGraph/Dify 二次开发、Git 协作和英文文档证据。
- 项目表达有时偏散，缺少“问题—方案—取舍—结果”的固定结构。

## 五、面试后跟进话术

面试后 1–2 个工作日可发：

> 您好，想跟进一下 AI 开发工程师岗位的面试进展。面试后我也重新梳理了文件与数据库一致性的设计，包括状态机、幂等重试和补偿任务，交流中收获很大。我对岗位和团队方向仍然很感兴趣，请问目前有进一步反馈或后续安排吗？

若 3–5 个工作日仍无回复，可再跟进一次：

> 您好，再跟进一下之前的面试进展。如果目前还在对比候选人，也麻烦告知大致时间安排，方便我协调后续面试，谢谢。

## 六、补强 To-do List

### P0：下一次面试前必须完成

- [ ] 能在 2 分钟内讲清机试项目：需求、设计、取舍、结果。
- [ ] 检查机试项目的重复删除、并发删除、半成功和宕机恢复。
- [ ] 能独立讲清 `ACTIVE → DELETING → DELETED/DELETE_FAILED` 状态机。
- [ ] 掌握幂等、重试、超时、补偿、对账和软删除。
- [ ] 准确复习 GIL、线程、进程、`asyncio`、CPU/IO 密集任务。
- [ ] 能画出 ReAct/Tool Calling Loop：Model → Tool Call → Execute → Observation → Model。
- [ ] 能对比 RAG、长上下文、Agent 渐进式搜索的适用场景和取舍。
- [ ] 准备“除了医疗还做过哪些项目”的 90 秒回答。

### P1：一周内完成

- [ ] 做一个最小 LangGraph Demo，包含规划、工具调用、状态、条件分支和失败重试。
- [ ] 增加最大循环次数、超时、人工确认和异常降级。
- [ ] 为 Demo 编写 README，包含架构、启动方式、测试方法和已知边界。
- [ ] 整理一个 AI 伪造测试数据的真实案例，说明如何发现和修正。
- [ ] 准备一个从模糊需求到最终交付的完整案例。
- [ ] 准备 3 个高质量反问，不只问工作时间和工作量。
- [ ] 进行两次 20 分钟模拟面试，检查是否存在重复、跑题和术语混淆。

## 七、后续回答的固定结构

技术题统一使用：

> **结论 → 原因 → 实现 → 失败场景 → 取舍**

项目题统一使用：

> **业务问题 → 本人职责 → 技术方案 → AI 如何参与 → 验收结果 → 已知问题**

不确定时不要猜，可以说：

> 这个场景我先确认一下负载类型和一致性要求。如果是……，我会选择……；如果是……，则需要……。

## 八、最终判断

这次不是全面失败。机试通过和 AI 编码工作流是强项，足以证明不是“只会讲概念”。技术面的失分集中在：

1. 跨资源一致性和后端可靠性设计。
2. Python 并发基础的准确性。
3. RAG、Agent 和 Tool Calling 的概念边界。

下一阶段不需要继续堆更多 AI 工具名称，而是要证明：

> **即使 AI 生成了大部分代码，我仍然知道为什么这样设计、会在哪里失败，以及如何把它做成可恢复、可评测、可交付的系统。**
